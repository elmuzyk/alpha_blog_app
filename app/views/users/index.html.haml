%h1.text-center All AlphaBlog Bloggers
- @records.each do |user|
  %ul.listing
    .row
      .well.col-md-4.col-md-offset-4.text-center
        %li
          = link_to gravatar_for(user), user_path(user)
        %li.article-title
          = link_to user.username, user_path(user)
        %li
          %small
            = pluralize(user.articles.count, 'article') if user.articles
        - if logged_in? and current_user.admin?
          %li= link_to "Delete user", user_path(user), method: :delete, data: { confirm: "Are you sure you want to delete this user and all his articles?" } 
